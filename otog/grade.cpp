#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int x;
    while(cin >> x) {
        if(x >= 50) printf("P\n");
        else printf("-\n");
    }
    return 0;
}